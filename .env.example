# Server
PORT=3000
SESSION_SECRET=change-this-to-a-long-random-string
BASE_URL=http://localhost:3000

# Venmo OAuth (legacy API - no longer open to new developers as of 2024)
# If you have legacy Venmo API credentials, set these to enable OAuth payments.
# Without them, the app falls back to Venmo deeplinks (manual click-to-pay, works for everyone).
# Apply/check status at: https://developer.venmo.com
VENMO_CLIENT_ID=
VENMO_CLIENT_SECRET=
